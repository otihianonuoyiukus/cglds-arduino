import { CSSProperties } from "react";

export const flexSpaceStyle: CSSProperties = {
  display: "flex",
};

export const unitBodyStyle: CSSProperties = {
  width: 300,
};

export const unitTitleStyle: CSSProperties = {
  textAlign: "left",
  margin: 0,
};

export const unitContentStyle: CSSProperties = {
  width: "100%",
  textAlign: "left",
};
